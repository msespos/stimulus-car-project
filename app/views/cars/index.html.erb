<%= form_with model: @car do |f| %>
  Select a car:<br>
  <%= f.collection_radio_buttons(:car_id, Car.order(:name), :id, :name) do |b| %>
    <%= b.label { b.radio_button + b.text } %><br>
  <% end %>
  <%= f.submit 'Select', class: "button" %>
<% end %>
