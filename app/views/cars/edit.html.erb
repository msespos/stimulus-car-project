<%# THIS NEEDS TO BE REWRITTEN TO DYNAMICALLY ADD VARIANT FIELDS %>
Make a car with three variants:
<%= form_with model: @car, method: :patch, local: true do |car_form| %>
  Name
  <%= car_form.text_field :name %><br><br>
  Variants:<br><br>
  <%= car_form.fields_for :variants do |variants_form| %>
    <%= variants_form.label :color %>
    <%= variants_form.text_field :color %><br>
    <%= variants_form.label :seats %>
    <%= variants_form.text_field :seats %><br>
    <%= variants_form.label :tape %>
    <%= variants_form.check_box :tape %><br><br>
  <% end %>
  <%= car_form.submit 'Make it!', class: "button" %>
<% end %>
